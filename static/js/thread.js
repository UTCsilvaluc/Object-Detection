// Entry point for the thread page.
// The implementation is split into small modules under `static/js/thread/`.

import { initThreadPageGlobals } from "./thread/index.js";

initThreadPageGlobals();

